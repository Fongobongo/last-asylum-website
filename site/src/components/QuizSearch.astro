---
import quizData from '../data/officialQuiz.json';
import type { Lang } from '../i18n/utils';

interface Props {
  lang?: Lang;
}

const { lang = 'ru' } = Astro.props;
const totalCount = quizData.length;

const i18n = {
  ru: {
    title: `🧠 Викторина мудрости: Шпаргалка ответов (${totalCount} вопросов)`,
    subtitle: 'Мгновенный поиск ответа во время 30-секундного таймера события «Испытание мудрости». Введите слово из вопроса или ответа ниже.',
    placeholder: '🔍 Введите ключевое слово (например: «Святилище», «Артур», «Ворон», «Селия»)...',
    all: `Все (${totalCount})`,
    events: 'События и Дуэль',
    heroes: 'Герои и Фракции',
    buildings: 'Здания',
    mechanics: 'Механики и Войска',
    lore: 'Сюжет и Лор',
    correctAnswer: 'Правильный ответ:',
    copyBtn: 'Копировать',
    copied: 'Скопировано!',
    found: 'Вопросов найдено:',
    noResults: 'Вопрос не найден. Попробуйте ввести другое ключевое слово.',
    clear: 'Очистить',
    timeTip: '⏱️ В игре дается 30 секунд на вопрос. Чем быстрее вы отвечаете вместе с альянсом, тем выше общая награда!',
  },
  en: {
    title: `🧠 Quiz of Wisdom: Complete Answer Key (${totalCount} Questions)`,
    subtitle: 'Lightning-fast answer lookup during the 30-second in-game trivia timer. Type any keyword from the question or options.',
    placeholder: '🔍 Type any keyword (e.g. "Sanctuary", "Arthur", "Raven", "Celia")...',
    all: `All (${totalCount})`,
    events: 'Events & Duel',
    heroes: 'Heroes & Factions',
    buildings: 'Buildings',
    mechanics: 'Mechanics & Troops',
    lore: 'Story & Lore',
    correctAnswer: 'Correct Answer:',
    copyBtn: 'Copy',
    copied: 'Copied!',
    found: 'Questions found:',
    noResults: 'No matching questions found. Try another keyword.',
    clear: 'Clear',
    timeTip: '⏱️ In-game timer is 30 seconds per question. Quick correct answers maximize alliance score and rewards!',
  },
  de: {
    title: `🧠 Quiz der Weisheit: Antwort-Datenbank (${totalCount} Fragen)`,
    subtitle: 'Sofortige Antwortsuche während des 30-Sekunden-Quiz-Events im Spiel.',
    placeholder: '🔍 Stichwort eingeben...',
    all: `Alle (${totalCount})`,
    events: 'Events & Duell',
    heroes: 'Helden',
    buildings: 'Gebäude',
    mechanics: 'Mechaniken',
    lore: 'Story & Lore',
    correctAnswer: 'Richtige Antwort:',
    copyBtn: 'Kopieren',
    copied: 'Kopiert!',
    found: 'Gefunden:',
    noResults: 'Keine passenden Fragen gefunden.',
    clear: 'Löschen',
    timeTip: '⏱️ 30 Sekunden Zeit pro Frage im Spiel.',
  },
  es: {
    title: `🧠 Quiz de Sabiduría: Guía de Respuestas (${totalCount} Preguntas)`,
    subtitle: 'Búsqueda rápida de respuestas durante el evento de 30 segundos.',
    placeholder: '🔍 Escribe una palabra clave...',
    all: `Todas (${totalCount})`,
    events: 'Eventos y Duelo',
    heroes: 'Héroes',
    buildings: 'Edifícios',
    mechanics: 'Mecánicas',
    lore: 'Historia y Lore',
    correctAnswer: 'Respuesta Correcta:',
    copyBtn: 'Copiar',
    copied: '¡Copiado!',
    found: 'Preguntas encontradas:',
    noResults: 'No se encontraron preguntas.',
    clear: 'Borrar',
    timeTip: '⏱️ El temporizador es de 30 segundos por pregunta.',
  },
  fr: {
    title: `🧠 Quiz de Sagesse : Réponses Officielles (${totalCount} Questions)`,
    subtitle: 'Recherche rapide de réponses pendant le compte à rebours de 30 secondes.',
    placeholder: '🔍 Tapez un mot-clé...',
    all: `Toutes (${totalCount})`,
    events: 'Événements & Duel',
    heroes: 'Héros',
    buildings: 'Bâtiments',
    mechanics: 'Mécanismes',
    lore: 'Histoire & Lore',
    correctAnswer: 'Bonne Réponse :',
    copyBtn: 'Copier',
    copied: 'Copié !',
    found: 'Questions trouvées :',
    noResults: 'Aucune question trouvée.',
    clear: 'Effacer',
    timeTip: '⏱️ 30 secondes par question en jeu.',
  },
  id: {
    title: `🧠 Kuis Kebijaksanaan: Kunci Jawaban Resmi (${totalCount} Soal)`,
    subtitle: 'Pencarian cepat jawaban kuis berwaktu 30 detik dalam game.',
    placeholder: '🔍 Ketik kata kunci...',
    all: `Semua (${totalCount})`,
    events: 'Event & Duel',
    heroes: 'Hero',
    buildings: 'Gedung',
    mechanics: 'Mekanik',
    lore: 'Cerita',
    correctAnswer: 'Jawaban Benar:',
    copyBtn: 'Salin',
    copied: 'Tersalin!',
    found: 'Soal ditemukan:',
    noResults: 'Soal tidak ditemukan.',
    clear: 'Hapus',
    timeTip: '⏱️ Waktu kuis 30 detik per soal.',
  },
  ja: {
    title: `🧠 知恵のクイズ：公式解答集（全${totalCount}問）`,
    subtitle: '制限時間30秒のクイズイベント用・キーワード即時検索ツール。',
    placeholder: '🔍 質問または選択肢のキーワードを入力...',
    all: `すべて (${totalCount})`,
    events: 'イベント・対決',
    heroes: '英雄・陣営',
    buildings: '施設・建築',
    mechanics: 'システム・兵士',
    lore: 'ストーリー・設定',
    correctAnswer: '正解：',
    copyBtn: 'コピー',
    copied: 'コピー済！',
    found: '該当数：',
    noResults: '一致する問題が見つかりません。',
    clear: 'クリア',
    timeTip: '⏱️ 1問あたり30秒の制限時間。同盟メンバーと協力して全問正解を目指しましょう。',
  },
  ko: {
    title: `🧠 지혜의 퀴즈: 공식 정답표 (총 ${totalCount}문항)`,
    subtitle: '게임 내 30초 제한시간 퀴즈 이벤트 즉시 검색 솔루션.',
    placeholder: '🔍 질문 또는 보기 키워드 검색...',
    all: `전체 (${totalCount})`,
    events: '이벤트 및 대결',
    heroes: '영웅 및 진영',
    buildings: '건물 및 시설',
    mechanics: '전투 및 시스템',
    lore: '스토리 및 배경',
    correctAnswer: '정답:',
    copyBtn: '복사',
    copied: '복사됨!',
    found: '검색된 문항:',
    noResults: '일치하는 질문이 없습니다.',
    clear: '초기화',
    timeTip: '⏱️ 문제당 30초 제한. 연맹원과 함께 빠르게 정답을 맞춰 최대 보상을 획득하세요.',
  },
  pt: {
    title: `🧠 Quiz da Sabedoria: Gabarito Oficial (${totalCount} Questões)`,
    subtitle: 'Busca rápida de respostas durante o evento de 30 segundos.',
    placeholder: '🔍 Digite uma palavra-chave da pergunta...',
    all: `Todas (${totalCount})`,
    events: 'Eventos e Duelo',
    heroes: 'Heróis',
    buildings: 'Edifícios',
    mechanics: 'Mecánicas',
    lore: 'História',
    correctAnswer: 'Resposta Correta:',
    copyBtn: 'Copiar',
    copied: 'Copiado!',
    found: 'Perguntas encontradas:',
    noResults: 'Nenhuma pergunta encontrada.',
    clear: 'Limpar',
    timeTip: '⏱️ 30 segundos por pergunta no jogo.',
  },
};

const t = i18n[lang as keyof typeof i18n] || i18n.en;

const categories = [
  { id: 'all', label: t.all },
  { id: 'events', label: t.events },
  { id: 'heroes', label: t.heroes },
  { id: 'buildings', label: t.buildings },
  { id: 'mechanics', label: t.mechanics },
  { id: 'lore', label: t.lore },
];

const formattedQuiz = (quizData as any[]).map((q) => {
  const title = q.title?.[lang] || q.title?.ru || q.title?.en || `Question #${q.id}`;
  const options = q.options.map((opt: any, idx: number) => {
    const text = opt?.[lang] || opt?.ru || opt?.en || `Option ${idx + 1}`;
    return {
      index: idx + 1,
      text,
      isCorrect: q.correctAnswer === idx + 1,
    };
  });
  const correctOption = options.find((o: any) => o.isCorrect) || options[0];

  const searchIndex = `${title} ${options.map((o: any) => o.text).join(' ')} ${q.title?.ru || ''} ${q.title?.en || ''}`.toLowerCase();

  return {
    id: q.id,
    category: q.category,
    title,
    options,
    correctText: correctOption.text,
    searchIndex,
  };
});
---

<section class="not-prose my-10 rounded-3xl border border-toxic-500/30 bg-stone-950/80 p-6 sm:p-8 backdrop-blur-xl shadow-[0_0_50px_rgba(0,0,0,0.6)]" id="quiz-lookup-app">
  <div class="mb-6 flex flex-col gap-2">
    <div class="flex items-center gap-3">
      <span class="rounded-xl border border-toxic-500/40 bg-toxic-950/60 px-3 py-1 text-xs font-bold text-toxic-400">
        {totalCount} OFFICIAL QUESTIONS
      </span>
      <span class="text-xs text-stone-500 font-mono">SMART CONTEST SOLVER</span>
    </div>
    <h2 class="text-2xl font-bold tracking-tight text-toxic-400 sm:text-3xl">{t.title}</h2>
    <p class="text-sm text-stone-400 max-w-3xl">{t.subtitle}</p>
  </div>

  <!-- Timer Advice Banner -->
  <div class="mb-6 rounded-2xl border border-stone-800 bg-stone-900/60 p-4">
    <p class="text-xs text-stone-300 leading-relaxed font-medium">{t.timeTip}</p>
  </div>

  <!-- Search and filter controls -->
  <div class="space-y-4">
    <div class="relative flex items-center">
      <input
        type="text"
        id="quiz-search-input"
        class="w-full rounded-2xl border border-stone-800 bg-stone-900/90 px-4 py-3.5 pl-11 text-base text-stone-100 placeholder-stone-500 transition-all focus:border-toxic-500 focus:bg-black focus:outline-none focus:ring-2 focus:ring-toxic-500/30"
        placeholder={t.placeholder}
        autocomplete="off"
      />
      <span class="absolute left-4 text-stone-400">🔍</span>
      <button
        id="quiz-clear-btn"
        class="absolute right-3 hidden rounded-lg bg-stone-800 px-2.5 py-1 text-xs text-stone-400 hover:bg-stone-700 hover:text-white transition"
      >
        {t.clear}
      </button>
    </div>

    <!-- Category Tabs -->
    <div class="flex flex-wrap gap-2 pt-1" id="quiz-categories">
      {categories.map((c) => (
        <button
          type="button"
          data-cat={c.id}
          class={`quiz-cat-btn rounded-xl px-3.5 py-1.5 text-xs font-semibold transition-all ${
            c.id === 'all'
              ? 'bg-toxic-500 text-stone-950 shadow-[0_0_15px_rgba(180,255,57,0.3)]'
              : 'border border-stone-800 bg-stone-900/60 text-stone-400 hover:border-toxic-500/40 hover:text-stone-200'
          }`}
        >
          {c.label}
        </button>
      ))}
    </div>

    <!-- Counter -->
    <div class="flex items-center justify-between text-xs text-stone-400 px-1">
      <div>
        {t.found} <span id="quiz-count" class="font-bold text-toxic-400">{formattedQuiz.length}</span>
      </div>
    </div>
  </div>

  <!-- Questions Grid -->
  <div class="mt-6 grid gap-4 sm:grid-cols-2" id="quiz-cards-container">
    {formattedQuiz.map((q) => (
      <div
        class="quiz-card flex flex-col justify-between rounded-2xl border border-stone-800/80 bg-stone-900/40 p-4 transition-all hover:border-toxic-500/50 hover:bg-stone-900/80 hover:shadow-lg"
        data-category={q.category}
        data-search={q.searchIndex}
      >
        <div>
          <div class="mb-2 flex items-center justify-between">
            <span class="rounded-md border border-stone-800 bg-stone-950 px-2 py-0.5 text-[10px] font-mono text-stone-400">
              #{q.id}
            </span>
            <span class="rounded-md border border-stone-800 bg-stone-950 px-2 py-0.5 text-[10px] font-medium uppercase tracking-wider text-stone-400">
              {q.category}
            </span>
          </div>

          <h3 class="text-sm font-bold text-stone-100 leading-snug">
            {q.title}
          </h3>

          <!-- All Options List -->
          <div class="mt-3 space-y-1.5">
            {q.options.map((opt: any) => (
              <div
                class={`flex items-center justify-between rounded-xl px-3 py-1.5 text-xs transition ${
                  opt.isCorrect
                    ? 'border border-toxic-500/40 bg-toxic-950/50 text-toxic-200 font-bold'
                    : 'border border-stone-800/60 bg-stone-950/40 text-stone-400'
                }`}
              >
                <span>
                  {opt.isCorrect ? '✅ ' : '• '}
                  {opt.text}
                </span>
                {opt.isCorrect && (
                  <span class="text-[10px] text-toxic-400 uppercase tracking-wide">
                    {lang === 'ru' ? 'ВЕРНО' : 'CORRECT'}
                  </span>
                )}
              </div>
            ))}
          </div>
        </div>

        <!-- Copy Answer Bar -->
        <div class="mt-4 pt-3 border-t border-stone-800/60 flex items-center justify-between text-xs">
          <div class="truncate max-w-[200px] text-stone-400">
            <span class="text-stone-500">{t.correctAnswer}</span> <strong class="text-toxic-300">{q.correctText}</strong>
          </div>
          <button
            type="button"
            class="quiz-copy-btn rounded-lg border border-stone-700 bg-stone-800/80 px-2.5 py-1 text-[11px] font-semibold text-stone-300 hover:bg-toxic-500 hover:text-stone-950 hover:border-toxic-500 transition"
            data-copy={q.correctText}
          >
            📋 {t.copyBtn}
          </button>
        </div>
      </div>
    ))}
  </div>

  <!-- No Results Box -->
  <div id="quiz-no-results" class="hidden mt-8 rounded-2xl border border-dashed border-stone-800 p-8 text-center text-sm text-stone-400">
    <p class="text-3xl mb-2">🔍</p>
    <p>{t.noResults}</p>
  </div>
</section>

<script>
  function initQuizSearch() {
    const input = document.getElementById('quiz-search-input') as HTMLInputElement | null;
    const clearBtn = document.getElementById('quiz-clear-btn');
    const cards = document.querySelectorAll<HTMLElement>('.quiz-card');
    const catBtns = document.querySelectorAll<HTMLButtonElement>('.quiz-cat-btn');
    const countSpan = document.getElementById('quiz-count');
    const noResults = document.getElementById('quiz-no-results');
    const copyBtns = document.querySelectorAll<HTMLButtonElement>('.quiz-copy-btn');

    if (!cards.length) return;

    let activeCategory = 'all';

    function filter() {
      const query = (input?.value || '').trim().toLowerCase();
      let visible = 0;

      cards.forEach((card) => {
        const cat = card.dataset.category || '';
        const searchTarget = card.dataset.search || '';

        const matchesCat = activeCategory === 'all' || cat === activeCategory;
        const matchesQuery = !query || searchTarget.includes(query);

        if (matchesCat && matchesQuery) {
          card.style.display = 'flex';
          visible++;
        } else {
          card.style.display = 'none';
        }
      });

      if (countSpan) countSpan.textContent = visible.toString();
      if (noResults) {
        noResults.style.display = visible === 0 ? 'block' : 'none';
      }
      if (clearBtn) {
        clearBtn.style.display = query.length > 0 ? 'block' : 'none';
      }
    }

    if (input) input.addEventListener('input', filter);

    if (clearBtn) {
      clearBtn.addEventListener('click', () => {
        if (input) {
          input.value = '';
          input.focus();
          filter();
        }
      });
    }

    catBtns.forEach((btn) => {
      btn.addEventListener('click', () => {
        const cat = btn.dataset.cat || 'all';
        activeCategory = cat;

        catBtns.forEach((b) => {
          if (b === btn) {
            b.classList.remove('border', 'border-stone-800', 'bg-stone-900/60', 'text-stone-400');
            b.classList.add('bg-toxic-500', 'text-stone-950', 'shadow-[0_0_15px_rgba(180,255,57,0.3)]');
          } else {
            b.classList.remove('bg-toxic-500', 'text-stone-950', 'shadow-[0_0_15px_rgba(180,255,57,0.3)]');
            b.classList.add('border', 'border-stone-800', 'bg-stone-900/60', 'text-stone-400');
          }
        });

        filter();
      });
    });

    // Copy Answer handling
    copyBtns.forEach((btn) => {
      btn.addEventListener('click', async () => {
        const text = btn.dataset.copy || '';
        if (text && navigator.clipboard) {
          await navigator.clipboard.writeText(text);
          const orig = btn.innerHTML;
          btn.innerHTML = '✅ OK';
          btn.classList.add('bg-toxic-500', 'text-stone-950');
          setTimeout(() => {
            btn.innerHTML = orig;
            btn.classList.remove('bg-toxic-500', 'text-stone-950');
          }, 1500);
        }
      });
    });
  }

  initQuizSearch();
  document.addEventListener('astro:page-load', initQuizSearch);
</script>
