---
import { quizQuestions } from '../data/quizQuestions';
import type { Lang } from '../i18n/utils';

interface Props {
  lang?: Lang;
}

const { lang = 'en' } = Astro.props;

const i18n = {
  ru: {
    title: '🧠 Шпаргалка по Викторине мудрости (15 из 15 верных ответов)',
    subtitle: 'Мгновенный поиск ответа по ключевому слову во время события в игре. Нажмите Ctrl + F или введите слово ниже.',
    placeholder: '🔍 Введите ключевое слово из вопроса (на русском или английском)...',
    all: 'Все',
    general: 'Общие знания',
    buildings: 'Здания',
    factions: 'Фракции',
    vip: 'VIP',
    lore: 'Лор и сюжет',
    events: 'События',
    heroes: 'Герои',
    alliance: 'Альянс',
    correctAnswer: 'Верный ответ:',
    found: 'Вопросов найдено:',
    noResults: 'Ничего не найдено. Попробуйте ввести другое ключевое слово (например: «святилище», «ворон», «селия»).',
    clear: 'Очистить',
  },
  en: {
    title: '🧠 Quiz of Wisdom Cheat Sheet (15/15 Correct Answers)',
    subtitle: 'Instant answer lookup during the in-game trivia event. Press Ctrl + F or type any keyword below.',
    placeholder: '🔍 Type any keyword from the question...',
    all: 'All',
    general: 'General Knowledge',
    buildings: 'Buildings',
    factions: 'Factions',
    vip: 'VIP',
    lore: 'Lore & Story',
    events: 'Events',
    heroes: 'Heroes',
    alliance: 'Alliance',
    correctAnswer: 'Correct Answer:',
    found: 'Questions found:',
    noResults: 'No questions matched your search. Try another keyword (e.g. "sanctuary", "raven", "celia").',
    clear: 'Clear',
  },
  de: {
    title: '🧠 Quiz der Weisheit Spickzettel (15/15 Richtige Antworten)',
    subtitle: 'Sofortige Antwortsuche während des Quiz-Events im Spiel. Nutze die Suchleiste unten.',
    placeholder: '🔍 Stichwort aus der Frage eingeben...',
    all: 'Alle',
    general: 'Allgemein',
    buildings: 'Gebäude',
    factions: 'Fraktionen',
    vip: 'VIP',
    lore: 'Story & Lore',
    events: 'Events',
    heroes: 'Helden',
    alliance: 'Allianz',
    correctAnswer: 'Richtige Antwort:',
    found: 'Gefundene Fragen:',
    noResults: 'Keine Treffer. Versuche ein anderes Stichwort.',
    clear: 'Zurücksetzen',
  },
  es: {
    title: '🧠 Guía Rápida del Quiz de la Sabiduría (15/15 Aciertos)',
    subtitle: 'Búsqueda instantánea de respuestas para el evento del juego. Usa la barra inferior.',
    placeholder: '🔍 Escribe una palabra clave de la pregunta...',
    all: 'Todos',
    general: 'Conocimiento General',
    buildings: 'Edificios',
    factions: 'Facciones',
    vip: 'VIP',
    lore: 'Historia y Lore',
    events: 'Eventos',
    heroes: 'Héroes',
    alliance: 'Alianza',
    correctAnswer: 'Respuesta correcta:',
    found: 'Preguntas encontradas:',
    noResults: 'No se encontraron coincidencias. Prueba con otra palabra clave.',
    clear: 'Limpiar',
  },
  fr: {
    title: '🧠 Antisèche du Quiz de la Sagesse (15/15 Bonnes Réponses)',
    subtitle: 'Recherche instantanée de réponses pendant l’événement du jeu. Utilisez la barre ci-dessous.',
    placeholder: '🔍 Tapez un mot-clé de la question...',
    all: 'Tous',
    general: 'Général',
    buildings: 'Bâtiments',
    factions: 'Factions',
    vip: 'VIP',
    lore: 'Histoire & Lore',
    events: 'Événements',
    heroes: 'Héros',
    alliance: 'Alliance',
    correctAnswer: 'Bonne réponse :',
    found: 'Questions trouvées :',
    noResults: 'Aucun résultat. Essayez un autre mot-clé.',
    clear: 'Effacer',
  },
  id: {
    title: '🧠 Kuis Kebijaksanaan: Kunci Jawaban (15/15 Benar)',
    subtitle: 'Cari jawaban instan selama event trivia berlangsung di dalam game.',
    placeholder: '🔍 Ketik kata kunci dari pertanyaan...',
    all: 'Semua',
    general: 'Pengetahuan Umum',
    buildings: 'Bangunan',
    factions: 'Fraksi',
    vip: 'VIP',
    lore: 'Kisah & Lore',
    events: 'Event',
    heroes: 'Pahlawan',
    alliance: 'Aliansi',
    correctAnswer: 'Jawaban Benar:',
    found: 'Pertanyaan ditemukan:',
    noResults: 'Tidak ada pertanyaan yang cocok. Coba kata kunci lain.',
    clear: 'Hapus',
  },
  ja: {
    title: '🧠 知恵のクイズ 攻略カンペ（15/15問全問正解）',
    subtitle: 'ゲーム内の知恵のクイズ開催中に素早く答えを引ける即時検索ツールです。',
    placeholder: '🔍 問題文のキーワードを入力（英日対応）...',
    all: 'すべて',
    general: '一般知識',
    buildings: '施設・建築',
    factions: '陣営・相性',
    vip: 'VIP',
    lore: 'ストーリー・世界観',
    events: 'イベント',
    heroes: '英雄',
    alliance: '同盟',
    correctAnswer: '正解：',
    found: '該当する問題数：',
    noResults: '一致する問題が見つかりません。別のキーワードをお試しください。',
    clear: 'クリア',
  },
  ko: {
    title: '🧠 지혜의 퀴즈 즉시 정답 족보 (15/15 만점 달성)',
    subtitle: '게임 내 지혜의 퀴즈 진행 시 즉시 검색하여 정답을 확인할 수 있는 실시간 검색기입니다.',
    placeholder: '🔍 질문의 핵심 키워드를 입력하세요...',
    all: '전체',
    general: '일반 상식',
    buildings: '건물/시설',
    factions: '진영 상성',
    vip: 'VIP',
    lore: '스토리/설정',
    events: '이벤트',
    heroes: '영웅',
    alliance: '연맹',
    correctAnswer: '정답:',
    found: '검색된 질문 수:',
    noResults: '일치하는 질문이 없습니다. 다른 키워드로 검색해 보세요.',
    clear: '초기화',
  },
  pt: {
    title: '🧠 Gabarito do Quiz da Sabedoria (15/15 Acertos)',
    subtitle: 'Busca rápida de respostas durante o evento de perguntas do jogo. Use o campo abaixo.',
    placeholder: '🔍 Digite uma palavra-chave da pergunta...',
    all: 'Todos',
    general: 'Geral',
    buildings: 'Edifícios',
    factions: 'Facções',
    vip: 'VIP',
    lore: 'História & Lore',
    events: 'Eventos',
    heroes: 'Heróis',
    alliance: 'Aliança',
    correctAnswer: 'Resposta Correta:',
    found: 'Perguntas encontradas:',
    noResults: 'Nenhuma pergunta encontrada. Tente outra palavra-chave.',
    clear: 'Limpar',
  },
};

const t = i18n[lang as keyof typeof i18n] || i18n.en;

const categories = [
  { id: 'all', label: t.all },
  { id: 'general', label: t.general },
  { id: 'buildings', label: t.buildings },
  { id: 'factions', label: t.factions },
  { id: 'vip', label: t.vip },
  { id: 'lore', label: t.lore },
  { id: 'events', label: t.events },
  { id: 'heroes', label: t.heroes },
  { id: 'alliance', label: t.alliance },
];
---

<section class="not-prose my-8 rounded-3xl border border-toxic-500/30 bg-stone-950/80 p-6 sm:p-8 backdrop-blur-xl shadow-[0_0_50px_rgba(0,0,0,0.6)]" id="quiz-lookup-app">
  <div class="mb-6 flex flex-col gap-2">
    <h2 class="text-2xl font-bold tracking-tight text-toxic-400 sm:text-3xl">{t.title}</h2>
    <p class="text-sm text-stone-400">{t.subtitle}</p>
  </div>

  <!-- Search and filter controls -->
  <div class="space-y-4">
    <div class="relative flex items-center">
      <input
        type="text"
        id="quiz-search-input"
        class="w-full rounded-2xl border border-stone-800 bg-stone-900/90 px-4 py-3.5 pl-11 text-base text-stone-100 placeholder-stone-500 transition-all focus:border-toxic-500 focus:bg-black focus:outline-none focus:ring-2 focus:ring-toxic-500/30"
        placeholder={t.placeholder}
        autocomplete="off"
      />
      <span class="absolute left-4 text-stone-400">🔍</span>
      <button
        id="quiz-clear-btn"
        class="absolute right-3 hidden rounded-lg bg-stone-800 px-2.5 py-1 text-xs text-stone-400 hover:bg-stone-700 hover:text-white transition"
      >
        {t.clear}
      </button>
    </div>

    <!-- Category Tabs -->
    <div class="flex flex-wrap gap-2 pt-1" id="quiz-categories">
      {categories.map((c) => (
        <button
          type="button"
          data-cat={c.id}
          class={`quiz-cat-btn rounded-xl px-3.5 py-1.5 text-xs font-semibold transition-all ${
            c.id === 'all'
              ? 'bg-toxic-500 text-stone-950 shadow-[0_0_15px_rgba(180,255,57,0.3)]'
              : 'border border-stone-800 bg-stone-900/60 text-stone-400 hover:border-toxic-500/40 hover:text-stone-200'
          }`}
        >
          {c.label}
        </button>
      ))}
    </div>

    <!-- Counter -->
    <div class="flex items-center justify-between text-xs text-stone-400 px-1">
      <div>
        {t.found} <span id="quiz-count" class="font-bold text-toxic-400">{quizQuestions.length}</span>
      </div>
    </div>
  </div>

  <!-- Questions Grid -->
  <div class="mt-6 grid gap-3 sm:grid-cols-2" id="quiz-cards-container">
    {quizQuestions.map((q) => {
      const isRu = lang === 'ru';
      const mainQ = isRu ? q.qRu : q.qEn;
      const subQ = isRu ? q.qEn : q.qRu;
      const mainA = isRu ? q.aRu : q.aEn;
      const subA = isRu ? q.aEn : q.aRu;

      return (
        <div
          class="quiz-card group flex flex-col justify-between rounded-2xl border border-stone-800/80 bg-stone-900/40 p-4 transition-all hover:border-toxic-500/40 hover:bg-stone-900/80 hover:shadow-lg"
          data-category={q.category}
          data-search={`${q.qEn} ${q.aEn} ${q.qRu} ${q.aRu}`.toLowerCase()}
        >
          <div>
            <div class="mb-2 flex items-center justify-between">
              <span class="rounded-md border border-stone-800 bg-stone-950 px-2 py-0.5 text-[10px] font-medium uppercase tracking-wider text-stone-400">
                {q.category}
              </span>
            </div>
            <h3 class="text-sm font-semibold text-stone-100 group-hover:text-toxic-200 leading-snug">
              {mainQ}
            </h3>
            {subQ && (
              <p class="mt-1 text-xs text-stone-500 italic">
                {subQ}
              </p>
            )}
          </div>

          <div class="mt-3 rounded-xl border border-toxic-500/30 bg-toxic-950/40 p-2.5">
            <div class="text-[11px] font-semibold text-toxic-400 uppercase tracking-wide">
              {t.correctAnswer}
            </div>
            <div class="text-sm font-bold text-toxic-200 mt-0.5">
              ✅ {mainA}
            </div>
            {subA && subA !== mainA && (
              <div class="text-xs text-stone-400 mt-0.5">
                {subA}
              </div>
            )}
          </div>
        </div>
      );
    })}
  </div>

  <!-- No Results Box -->
  <div id="quiz-no-results" class="hidden mt-8 rounded-2xl border border-dashed border-stone-800 p-8 text-center text-sm text-stone-400">
    <p class="text-3xl mb-2">🔍</p>
    <p>{t.noResults}</p>
  </div>
</section>

<script>
  function initQuizSearch() {
    const input = document.getElementById('quiz-search-input') as HTMLInputElement | null;
    const clearBtn = document.getElementById('quiz-clear-btn');
    const cards = document.querySelectorAll<HTMLElement>('.quiz-card');
    const catBtns = document.querySelectorAll<HTMLButtonElement>('.quiz-cat-btn');
    const countSpan = document.getElementById('quiz-count');
    const noResults = document.getElementById('quiz-no-results');

    if (!input || !cards.length) return;

    let activeCategory = 'all';

    function filter() {
      const query = (input?.value || '').trim().toLowerCase();
      let visible = 0;

      cards.forEach((card) => {
        const cat = card.dataset.category || '';
        const searchTarget = card.dataset.search || '';

        const matchesCat = activeCategory === 'all' || cat === activeCategory;
        const matchesQuery = !query || searchTarget.includes(query);

        if (matchesCat && matchesQuery) {
          card.style.display = 'flex';
          visible++;
        } else {
          card.style.display = 'none';
        }
      });

      if (countSpan) countSpan.textContent = visible.toString();
      if (noResults) {
        noResults.style.display = visible === 0 ? 'block' : 'none';
      }
      if (clearBtn) {
        clearBtn.style.display = query.length > 0 ? 'block' : 'none';
      }
    }

    input.addEventListener('input', filter);

    if (clearBtn) {
      clearBtn.addEventListener('click', () => {
        if (input) {
          input.value = '';
          input.focus();
          filter();
        }
      });
    }

    catBtns.forEach((btn) => {
      btn.addEventListener('click', () => {
        const cat = btn.dataset.cat || 'all';
        activeCategory = cat;

        catBtns.forEach((b) => {
          if (b === btn) {
            b.classList.remove('border', 'border-stone-800', 'bg-stone-900/60', 'text-stone-400');
            b.classList.add('bg-toxic-500', 'text-stone-950', 'shadow-[0_0_15px_rgba(180,255,57,0.3)]');
          } else {
            b.classList.remove('bg-toxic-500', 'text-stone-950', 'shadow-[0_0_15px_rgba(180,255,57,0.3)]');
            b.classList.add('border', 'border-stone-800', 'bg-stone-900/60', 'text-stone-400');
          }
        });

        filter();
      });
    });
  }

  // Init on DOM load and Astro view transitions
  initQuizSearch();
  document.addEventListener('astro:page-load', initQuizSearch);
</script>
